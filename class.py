import sys, pygame

from pygame.locals import *
from connection import *

class Card(object):
    def __init__():
        self.info = GetCardInfo(name)
        self.name = name
        self.visible = False
        self.img = pygame.imga.load('yugi_lib/images/cards/' + nome + '.png')
